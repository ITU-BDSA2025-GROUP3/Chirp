@page "/{author}"
@model Chirp.Web.Pages.UserTimelineModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Chirp!";
    Layout = "Shared/_Layout";
    var routeName = HttpContext.GetRouteValue("author")?.ToString();
}

<div>
    <h2> @routeName's Timeline </h2>
    @if (User.Identity!.IsAuthenticated && string.Equals(User.Identity.Name, routeName, StringComparison.OrdinalIgnoreCase))
    {
        <div class="cheepbox">
            <h3>What's on your mind @(User.Identity.Name)?</h3>
            <form method="post">
                <input style="float: left" type="text" asp-for="Message">
                <button type="submit" style="height: 22px; width: 70px; margin-left: 5px;background-color: #571010;font-weight: bold;border: 1px solid #3b0707;font-size: 14px;font-family: 'Trebuchet MS', sans-serif;border-radius: 2px; padding: 0">Submit</button>
                <span asp-validation-for="Message" style="color: red; display: inline-flex"></span>
            </form>
        </div>
    }
    
    @if (Model.Cheeps.Any())
    {
        <ul id="messagelist" class="cheeps">
            @foreach (var cheep in Model.Cheeps)
            {
                <li style="display: inline-flex;
                    flex-direction: row;
                    justify-content: space-between;
                    align-items: center;
                    width: 98%;">
                    <p>
                        <strong>
                            <a href="/@cheep.Author">@cheep.Author</a>
                        </strong>
                        @cheep.Message
                        <small>&mdash; @cheep.TimeStamp</small>
                    </p>
                    @if (User.Identity!.IsAuthenticated)
                    {
                            @* TODO fix author with follower comparison *@
                        @if (User.Identity.Name != cheep.Author)
                        {
                        // TODO remove button UI once followed check is implemented
                            <button type="button" asp-page-handler="Follow" asp-for="Follower">Follow</button>
                            @* @if (!followed) *@
                            @* { *@
                            @* <button type="button" asp-page-handler="Follow" asp-for="Follower">Follow</button> *@
                            @* }else *@
                            @* { *@
                            @* <button type="button" asp-page-handler="Unfollow" asp-for="Follower">Unfollow</button> *@
                            @* } *@
                        }   
                    }
                    
                </li>
            }
        </ul>
    }
    else
    {
        <em>There are no cheeps so far.</em>
    }
    
    @if (Model.TotalAuthorPages > 1)
    {
        <nav class="pagination">
            @if (Model.CurrentPage > 1)
            {
                <a class="page-link" href="?page=@(Model.CurrentPage - 1)"
                >Previous
                </a>
            }
            else
            {
                <span class="page-link disabled">Previous</span>
            }
            <span>Page @Model.CurrentPage of @Model.TotalAuthorPages</span>
            @if (Model.CurrentPage < Model.TotalAuthorPages)
            {
                <a class="page-link" href="?page=@(Model.CurrentPage + 1)"
                >Next
                </a>
            }
            else
            {
                <span class="page-link disabled">Next</span>
            }
        </nav>
    }
</div>